# Copy to .env and fill in to enable the real Claude pipeline.
# Without a key the app runs entirely on the rule-based mock (fully demoable).
ANTHROPIC_API_KEY=

# Optional overrides
TGA_MODEL=claude-opus-4-8
# Force mock even when a key is present: TGA_FORCE_MOCK=1
